# name conventions used in the core ir:
# - functions: f<id>
# - blocks: b<id>
# - instructions: %<id>
# - memory: m<id>
# - free variables: ^<id>
# - function parameters: $f<pid>
# - block parameters: $b<bid>.<pid>
.core
.fn getz.72 #f2 : fn((double, double, double)) -> double
.freevars ()
.entry b0 {
  .bb #b0 () {
    let %0: double = load double field($f0, 0 * @(@64, @(@64, @(@64, @32))))
    let %1: double = load double field($f0, 1 * @(@64, @(@64, @(@64, @32))))
    let %2: double = load double field($f0, 2 * @(@64, @(@64, @(@64, @32))))
    return %2
  }
}
.fn gety.71 #f1 : fn((double, double, double)) -> double
.freevars ()
.entry b0 {
  .bb #b0 () {
    let %0: double = load double field($f0, 0 * @(@64, @(@64, @(@64, @32))))
    let %1: double = load double field($f0, 1 * @(@64, @(@64, @(@64, @32))))
    let %2: double = load double field($f0, 2 * @(@64, @(@64, @(@64, @32))))
    return %1
  }
}
.fn main.73 #f3 : fn() -> unit
.freevars ()
.entry b0 {
  .bb #b0 () {
    let _ = alloc <m0: (double, double, double) heap(3)> <- [0] = 1, [1] = 2, [2] = 3
    let _ = alloc <m1: (double, double, double) heap(3)> <- [0] = 4, [1] = 5, [2] = 6
    let %2: double = call f0(f3.m0)
    let %3: double = call f0(f3.m1)
    let %4: double = mul(%2, %3)
    let %5: double = call f1(f3.m0)
    let %6: double = call f1(f3.m1)
    let %7: double = mul(%5, %6)
    let %8: double = add(%4, %7)
    let %9: double = call f2(f3.m0)
    let %10: double = call f2(f3.m1)
    let %11: double = mul(%9, %10)
    let %12: double = add(%8, %11)
    let %13: double = mul(1000000, %12)
    let %14: int32 = conv.d2i.rtz(%13)
    let %15: unit = ext_call minimbt_print_int(%14)
    return %15
  }
}
.fn getx.70 #f0 : fn((double, double, double)) -> double
.freevars ()
.entry b0 {
  .bb #b0 () {
    let %0: double = load double field($f0, 0 * @(@64, @(@64, @(@64, @32))))
    let %1: double = load double field($f0, 1 * @(@64, @(@64, @(@64, @32))))
    let %2: double = load double field($f0, 2 * @(@64, @(@64, @(@64, @32))))
    return %0
  }
}
.fn T$start #f8 : fn() -> unit
.freevars ()
.entry b0 {
  .bb #b0 () {
    let %0: unit = call f3()
    return %0
  }
}
.thunk ::0 start
