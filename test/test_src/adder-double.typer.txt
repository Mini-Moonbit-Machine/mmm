let rec make_adder: (x: Double) -> Fun([Int], Double) {
  let rec adder: (y: Int) -> Double {
    Var("x") + App(Var("float_of_int"), [Var("y")])
  }
  Var(adder)
}
let rec main: () -> Var({val: Some(Unit)}) {
  let add: Var({val: Some(Fun([Int], Double))}) = Apply(Var("make_adder"), [Double(2.14), ])
  Apply(Var("print_int"), [App(Var("int_of_float"), [App(Var("add"), [Int(1)])]), ])
}
Unit
