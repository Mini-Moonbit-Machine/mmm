let rec main.1: Var({val: Some(Unit)}) = () {
  let rec f.2: Var({val: Some(Unit)}) = () {
    let join f.6: Var({val: Some(Unit)}) = () {
      let x.7: Int = ExtApply(minimbt_read_int, [])
      let _8: Int = Int(-2147483648)
      if (x.7 == _8) then {
        ExtApply(minimbt_print_int, [x.7, ])
      } else {
        let _9: Unit = ExtApply(minimbt_print_int, [x.7, ])
        Jump(f.6, [])
      }
    }
    Jump(f.6, [])
  }
  Apply(f.2, [])
}
Apply(main.1, [])