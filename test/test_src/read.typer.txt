let rec main: () -> Var(Unit) {
  let rec f: () -> Var(Unit) {
    let x: Var(Int) = Apply(Var("read_int"), [])
    if (Eq(Var("x"), Neg(Int(2147483648), kind=Some(Int)))) then {
      Apply(Var("print_int"), [Var("x"), ])
    } else {
      let _: Var(Unit) = Apply(Var("print_int"), [Var("x"), ])
      Apply(Var("f"), [])
    }
  }
  Apply(Var("f"), [])
}
Unit
