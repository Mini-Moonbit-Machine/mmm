# name conventions used in the core ir:
# - functions: f<id>
# - blocks: b<id>
# - instructions: %<id>
# - memory: m<id>
# - free variables: ^<id>
# - function parameters: $f<pid>
# - block parameters: $b<bid>.<pid>
.core
.fn T$start #f4 : fn() -> unit
.freevars ()
.entry b0 {
  .bb #b0 () {
    let %0: unit = call f0()
    return %0
  }
}
.fn adder4.14 #f2 : closure(int32) -> double
.freevars (double)
.entry b0 {
  .bb #b0 () {
    let %0: double = conv.i2d($f0)
    let %1: double = add(^1, %0)
    return %1
  }
}
.fn main8 #f0 : fn() -> unit
.freevars ()
.entry b0 {
  .bb #b0 () {
    let _ = alloc <m0: closure(int32) -> double heap(2)> <- [0] = f2, [1] = 2.14
    let %1: double = apply f0.m0(1)
    let %2: int32 = conv.d2i.rtz(%1)
    let %3: unit = ext_call minimbt_print_int(%2)
    return %3
  }
}
.thunk ::0 start
