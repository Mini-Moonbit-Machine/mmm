let rec f: (x: Int) -> Int {
  Var("x") + Int(123)
}
let rec g: (y: Int) -> (Int) -> Int {
  Var(f)
}
let rec main: () -> Var(Unit) {
  Apply(Var("print_int"), [App(App(Var("g"), [Int(456)]), [Int(789)]), ])
}
Unit
